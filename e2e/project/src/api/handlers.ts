// Placeholder.
//
// The fixture needs directories that exist on disk: a declared component whose
// path is missing is a warning, and `component owners` has to have something to
// resolve. Nothing here is compiled or read — the records are the fixture.
export const surface = "api";
