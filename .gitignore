# The binary a plain `go build` drops in the root.
/openrecord

# goreleaser's output.
/dist/

# A store this repository grows for its own records — anchored to the root, so
# the fixture project under e2e/ carries its store into version control like any
# real project does.
/.openrecord/
